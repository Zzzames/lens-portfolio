# 快速测试步骤

## ⚡ 5分钟快速测试

### 步骤1：启动服务器（30秒）

**Windows：**
```
双击 START-SERVER.bat
```

**Mac/Linux：**
```bash
chmod +x START-SERVER.sh
./START-SERVER.sh
```

**验证：** 看到 "服务器已启动" 消息

---

### 步骤2：打开管理工具（10秒）

浏览器访问：
```
http://localhost:8000/admin/
```

**验证：** 看到管理工具界面

---

### 步骤3：同步图片（1分钟）

1. 点击顶部 **"同步网站图片"** 按钮
2. 等待扫描完成
3. 看到图片列表（默认为列表视图）

**验证：** 
- 列表中显示了图片
- 每张图片有标题和描述
- 显示为列表视图（不是网格视图）

---

### 步骤4：编辑一张图片（1分钟）

1. 找到第一张图片
2. 点击 **"编辑"** 按钮
3. 修改标题为：`测试·修改标题`
4. 修改描述为：`这是一个测试修改`
5. 点击 **"保存"**

**验证：**
- 看到 "✓ 保存成功" 提示
- 列表中显示修改后的标题和描述
- 终端显示 "✓ 元数据已保存"

---

### 步骤5：查看网站效果（1分钟）

打开对应的作品集页面，例如：
```
http://localhost:8000/pages/street.html
```

**强制刷新页面** - `Ctrl + F5` (Windows) 或 `Cmd + Shift + R` (Mac)

**验证：**
- 第一张图片显示修改后的标题：`测试·修改标题`
- 鼠标悬停时显示修改后的描述：`这是一个测试修改`

---

### 步骤6：查看元数据文件（30秒）

打开文件：
```
data/site-images-metadata.json
```

**验证：**
- 文件存在
- JSON格式正确
- 包含刚才修改的内容
- 有备份文件 `site-images-metadata.backup.json`

---

## ✅ 测试检查清单

完成以下所有项目即表示测试成功：

### 服务器测试
- [ ] API服务器成功启动
- [ ] 可以访问 `http://localhost:8000`
- [ ] 可以访问 `http://localhost:8000/admin/`

### 管理工具测试
- [ ] 同步图片功能正常
- [ ] 默认显示为列表视图
- [ ] 列表项只显示标题和描述（无分类、文件名等）
- [ ] 可以编辑图片信息
- [ ] 保存时显示成功提示
- [ ] 终端显示 "✓ 元数据已保存"

### 网站前端测试
- [ ] 作品集页面可以打开
- [ ] 显示修改后的标题
- [ ] 显示修改后的描述
- [ ] 未修改的图片显示默认标题和描述

### 数据文件测试
- [ ] `data/site-images-metadata.json` 文件存在
- [ ] JSON格式正确
- [ ] 包含修改后的数据
- [ ] 备份文件自动创建

---

## 🐛 常见测试问题

### 问题：服务器启动失败

**检查：**
```bash
python --version
# 或
python3 --version
```

确保Python 3.x已安装

---

### 问题：点击"同步图片"后没有反应

**打开浏览器控制台（F12）** 查看错误信息

可能原因：
1. `images/` 目录不存在
2. 图片命名格式不正确
3. 没有 `.webp` 格式的图片

---

### 问题：保存后显示 "501 错误"

**原因：** API服务器没有运行

**解决：** 
1. 停止当前服务器（Ctrl+C）
2. 重新运行 `START-SERVER.bat` 或 `START-SERVER.sh`
3. 确认看到 "服务器已启动" 消息

---

### 问题：网站没有显示修改后的内容

**解决步骤：**
1. **强制刷新** - `Ctrl + F5`
2. **清除缓存** - 浏览器设置 → 清除浏览数据
3. **检查元数据文件** - 打开 `data/site-images-metadata.json` 确认内容
4. **查看浏览器控制台** - F12 → Console，查看是否有加载错误

---

## 🔍 深度测试

如果基础测试都通过，可以进行以下深度测试：

### 1. 批量修改测试
- 同时选择多张图片
- 批量删除（不推荐在真实数据上测试）

### 2. 筛选和搜索测试
- 按分类筛选
- 搜索关键词
- 组合使用筛选和搜索

### 3. 视图切换测试
- 切换到网格视图
- 再切换回列表视图
- 验证数据显示正确

### 4. 数据恢复测试
- 手动导出元数据
- 修改一些数据
- 从备份恢复

### 5. 并发测试
- 打开多个浏览器标签
- 同时编辑不同图片
- 验证数据一致性

---

## 📊 测试报告模板

测试完成后，可以填写以下报告：

```
测试日期：_______
测试人员：_______
测试环境：Windows/Mac/Linux + Chrome/Firefox/Safari

【服务器测试】
✅ / ❌  服务器启动
✅ / ❌  API响应正常

【管理工具测试】
✅ / ❌  图片同步
✅ / ❌  图片编辑
✅ / ❌  数据保存
✅ / ❌  列表视图默认

【网站前端测试】
✅ / ❌  标题显示
✅ / ❌  描述显示
✅ / ❌  元数据加载

【问题记录】
1. _______________
2. _______________

【总体评价】
✅ 测试通过，可以上线
❌ 有问题，需要修复
```

---

**测试建议：** 先在测试环境完成所有测试，确认无误后再部署到生产环境！

