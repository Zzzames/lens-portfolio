# Admin工具快速开始指南 v2.1 ⚡

## 🎉 新版本亮点

✅ **自动保存** - 无需手动下载替换文件  
✅ **零404错误** - 优化扫描，仅检测webp格式  
✅ **一键启动** - 双击即可启动服务器  

---

## 🚀 3步快速开始

### 第一步：启动服务器

**Windows用户**：
```
双击 START-SERVER.bat
```

**Mac/Linux用户**：
```bash
chmod +x START-SERVER.sh
./START-SERVER.sh
```

**或手动启动**：
```bash
python api-server.py
# 或指定端口
python api-server.py 8001
```

### 第二步：访问管理工具

浏览器打开：
```
http://localhost:8000/admin/
```

### 第三步：同步图片

1. 点击左侧 **"管理图片"** 菜单
2. 点击右上角 **"🔄 同步网站图片"** 按钮
3. 等待10-30秒扫描完成
4. 完成！现在可以看到所有图片

---

## ✨ 核心功能

### 1️⃣ 查看和编辑图片

**查看图片**：
- 在"管理图片"页面浏览所有图片
- 使用分类筛选查看特定类别
- 搜索框按标题或描述搜索

**编辑图片**：
1. 点击图片的 **"编辑"** 按钮
2. 修改标题、描述或分类
3. 点击 **"保存更改"**
4. ✅ **自动保存** - 无需任何额外操作！

**查看大图**：
- 直接点击图片查看完整信息和大图预览

### 2️⃣ 批量操作

1. 勾选多张图片（左上角复选框）
2. 使用底部批量操作栏
3. 批量删除或修改
4. ✅ **自动保存**

### 3️⃣ 搜索和筛选

- **分类筛选**：顶部下拉菜单选择分类
- **关键词搜索**：输入关键词实时搜索
- **视图切换**：网格/列表两种查看模式

### 4️⃣ 数据备份

- 点击"备份恢复" → "导出数据"
- 下载完整的元数据JSON文件
- 用于数据备份或迁移

---

## 🎯 与v2.0的主要区别

| 功能 | v2.0 | v2.1 (当前) |
|------|------|------------|
| 保存方式 | 手动下载替换 | ✅ 自动保存 |
| 404错误 | 大量404请求 | ✅ 零404 |
| 服务器启动 | 手动命令 | ✅ 双击启动 |
| 扫描速度 | 较慢（检测4种格式） | ✅ 快速（仅webp） |

---

## 📋 常见操作

### 修改图片信息
```
管理图片 → 找到图片 → 编辑 → 修改 → 保存 → ✅自动完成
```

### 添加新图片到网站
```
1. 将webp图片放到 images/分类名/ 目录
2. 按命名规则命名（如 street-13.webp）
3. Admin工具中点击"同步网站图片"
4. 编辑图片信息
5. ✅自动保存
```

### 删除图片
```
1. 在Admin工具中删除（从元数据移除）
2. ✅自动保存
3. 手动删除 images/ 目录中的实际文件（可选）
```

---

## ⚙️ 技术说明

### 自动保存原理

```
编辑保存
    ↓
前端调用 /api/save-metadata
    ↓
Python后端接收请求
    ↓
自动备份旧文件
    ↓
写入新的元数据文件
    ↓
返回成功响应
    ↓
✅完成
```

### 文件说明

- **api-server.py** - Python后端服务器（自动保存API）
- **START-SERVER.bat** - Windows启动脚本
- **START-SERVER.sh** - Mac/Linux启动脚本
- **data/site-images-metadata.json** - 元数据文件
- **data/site-images-metadata.backup.json** - 自动备份

### 命名规则

只支持webp格式，命名规则：

```
street-01.webp, street-02.webp, ...    # 街头摄影
doc-01.webp, doc-02.webp, ...          # 纪实摄影
nature-01.webp, nature-02.webp, ...    # 自然风光
portrait-01.webp, portrait-02.webp, ... # 人像摄影
city-01.webp, city-02.webp, ...        # 城市景观
animals-01.webp, animals-02.webp, ...  # 动物摄影
still-01.webp, still-02.webp, ...      # 静物摄影
```

---

## 🐛 故障排除

### Q: 启动服务器失败？
**A**: 
- 确认已安装Python 3.x
- 尝试更换端口：`python api-server.py 8001`
- 检查8000端口是否被占用

### Q: 修改后主网站没更新？
**A**:
- 确认服务器正在运行（控制台有"✓ 元数据已保存"提示）
- 刷新浏览器清除缓存（Ctrl+Shift+R）
- 检查`data/site-images-metadata.json`文件时间戳

### Q: 同步找不到图片？
**A**:
- 确认图片格式为webp
- 检查文件名符合命名规则
- 确认图片在正确的目录（`images/分类名/`）

### Q: 看不到保存成功提示？
**A**:
- 检查浏览器控制台（F12）
- 确认服务器使用`api-server.py`启动
- 如果使用其他HTTP服务器，需要手动下载元数据

---

## 💡 最佳实践

### 1. 定期备份
- 每周导出一次元数据备份
- 保存在安全的地方

### 2. 图片管理
- 新图片按顺序编号
- 及时同步和编辑信息
- 定期检查是否有未使用的图片

### 3. 分类管理
- 合理使用7个预设分类
- 保持每个分类的图片数量平衡
- 描述信息尽量详细

---

## 🎓 进阶技巧

### 自定义端口
```bash
python api-server.py 9000
# 然后访问 http://localhost:9000/admin/
```

### 查看API日志
服务器控制台会显示所有API请求：
```
✓ 元数据已保存: data/site-images-metadata.json
  - 版本: 1.0
  - 最后更新: 2025-10-04T...
  - 图片总数: 89
```

### 恢复备份
如果修改出错：
```
1. 停止服务器
2. 删除 data/site-images-metadata.json
3. 重命名 data/site-images-metadata.backup.json 为 site-images-metadata.json
4. 重启服务器
```

---

## 🆘 需要帮助？

1. 查看浏览器控制台（F12）的错误信息
2. 查看服务器控制台的日志
3. 检查`data/`目录的文件权限
4. 确认Python版本 >= 3.6

---

## 🎉 开始使用

现在你已经了解了所有功能，开始管理你的摄影网站吧！

**记住三步**：
1. 双击启动服务器
2. 浏览器访问 http://localhost:8000/admin/
3. 点击"同步网站图片"

一切就是这么简单！✨

---

*最后更新: 2025-10-04*  
*版本: v2.1.0*

