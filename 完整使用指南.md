# 摄影网站 - 完整使用指南

## 📋 系统架构说明

### 数据流程图

```
管理工具 (admin/index.html)
    ↓ 修改标题/描述
    ↓
API服务器 (api-server.py)
    ↓ POST /api/save-metadata
    ↓
元数据文件 (data/site-images-metadata.json)
    ↓ 读取
    ↓
网站前端 (unified-image-loader.js)
    ↓ 显示
    ↓
用户看到的网站
```

### 关键概念

1. **默认数据** - 硬编码在 `unified-image-loader.js` 中的标题和描述
2. **自定义数据** - 通过管理工具修改后保存在 `data/site-images-metadata.json` 中
3. **数据优先级** - 自定义数据 > 默认数据

---

## 🚀 快速开始

### 第1步：启动服务器

**Windows系统：**
```bash
# 双击运行
START-SERVER.bat

# 或在命令行运行
python api-server.py
```

**Mac/Linux系统：**
```bash
# 添加执行权限（首次）
chmod +x START-SERVER.sh

# 运行
./START-SERVER.sh

# 或直接运行
python3 api-server.py
```

**成功启动后会看到：**
```
========================================================
🚀 摄影网站Admin工具 - API服务器
========================================================

✓ 服务器已启动: http://localhost:8000
✓ Admin工具地址: http://localhost:8000/admin/
✓ API端点: http://localhost:8000/api/save-metadata

功能：
  - 静态文件服务（整个网站）
  - 自动保存元数据API
  - 自动备份功能

按 Ctrl+C 停止服务器
========================================================
```

### 第2步：访问管理工具

在浏览器中打开：
```
http://localhost:8000/admin/
```

### 第3步：同步现有图片

1. 点击顶部的 **"同步网站图片"** 按钮
2. 等待扫描完成（会自动扫描所有分类的 `.webp` 图片）
3. 扫描完成后，列表中会显示所有图片及其默认的标题和描述

### 第4步：编辑图片信息

1. 在列表中找到要编辑的图片
2. 点击 **"编辑"** 按钮
3. 修改标题和描述
4. 点击 **"保存"** - 会自动保存到服务器

### 第5步：查看网站效果

在浏览器中打开任意作品集页面，例如：
```
http://localhost:8000/pages/street.html
```

刷新页面，您会看到修改后的标题和描述！

---

## 📊 详细功能说明

### 1. 同步网站图片

**功能：** 扫描 `images/` 目录下的所有图片，建立元数据

**操作步骤：**
1. 点击顶部 "同步网站图片" 按钮
2. 等待进度提示（会显示 "正在扫描xxx分类..."）
3. 扫描完成后，自动切换到列表视图

**注意事项：**
- 只扫描 `.webp` 格式的图片
- 图片命名格式：`{prefix}-{编号}.webp`
- 编号为两位数，如 `01`, `02`, ... `23`

**支持的分类和前缀：**
| 分类中文名 | 分类ID | 文件前缀 | 示例文件名 |
|-----------|--------|---------|-----------|
| 街头摄影 | street | street | street-01.webp |
| 人文纪实 | documentary | doc | doc-01.webp |
| 自然风光 | nature | nature | nature-01.webp |
| 城市风光 | cityscape | city | city-01.webp |
| 静物时光 | stilllife | still | still-01.webp |
| 人像摄影 | portrait | portrait | portrait-01.webp |
| 动物摄影 | animals | animals | animals-01.webp |

---

### 2. 编辑图片信息

**列表视图（默认）：**
- 显示图片缩略图、标题、描述
- 每个图片有 "编辑" 和 "删除" 按钮

**网格视图：**
- 卡片式展示
- 点击卡片可以查看大图

**编辑对话框字段：**
- **标题** - 图片标题（必填）
- **描述** - 详细描述（可选）
- **分类** - 所属分类（自动填充，可修改）

**保存逻辑：**
```javascript
点击保存
  ↓
发送到 API: POST /api/save-metadata
  ↓
API 保存到 data/site-images-metadata.json
  ↓
创建备份到 data/site-images-metadata.backup.json
  ↓
返回成功消息
  ↓
前端显示 "✓ 保存成功"
```

---

### 3. 筛选和搜索

**分类筛选：**
- 下拉菜单选择分类
- 只显示该分类的图片

**关键词搜索：**
- 在搜索框输入关键词
- 实时搜索标题和描述
- 支持中文、英文、数字

**示例：**
- 搜索 "天津" - 显示所有标题或描述中包含"天津"的图片
- 筛选 "街头摄影" - 只显示街头摄影分类

---

### 4. 批量操作

**选择图片：**
- 点击图片左上角的复选框
- 顶部显示已选择数量

**批量删除：**
1. 选择多张图片
2. 点击 "批量删除" 按钮
3. 确认删除

**全选/取消全选：**
- 点击 "全选" 按钮选择当前页所有图片
- 再次点击取消全选

---

### 5. 数据导出（手动备份）

**功能：** 将元数据导出为JSON文件，用于备份或迁移

**操作步骤：**
1. 点击 "手动导出元数据" 按钮
2. 浏览器会下载 `site-images-metadata-{时间戳}.json` 文件
3. 保存到安全位置

**何时使用：**
- 重大修改前
- 定期备份
- 迁移到新服务器前

---

### 6. 数据恢复

**自动备份：**
- 每次保存时，自动创建 `data/site-images-metadata.backup.json`
- 只保留最新一次备份

**手动恢复：**
1. 找到备份文件（`.backup.json` 或手动导出的文件）
2. 复制内容
3. 打开浏览器开发者工具（F12）
4. 进入 Console 标签
5. 执行恢复命令（待实现导入功能）

**或者直接替换文件：**
```bash
# 停止服务器
# 复制备份文件
cp data/site-images-metadata.backup.json data/site-images-metadata.json
# 重启服务器
```

---

## 🔧 故障排查

### 问题1：保存时提示 "501 Unsupported method"

**原因：** API服务器没有正确启动

**解决方案：**
1. 检查服务器是否在运行
2. 重新运行 `START-SERVER.bat` 或 `START-SERVER.sh`
3. 确认看到 "服务器已启动" 消息

---

### 问题2：管理工具修改后，网站没有更新

**原因：** 浏览器缓存或元数据未加载

**解决方案：**
1. **强制刷新网站页面** - `Ctrl + F5` (Windows) 或 `Cmd + Shift + R` (Mac)
2. **检查元数据文件是否存在** - 打开 `data/site-images-metadata.json`
3. **检查文件内容** - 确认修改已保存
4. **查看浏览器控制台** - 打开开发者工具（F12），查看是否有错误

---

### 问题3：图片标题和描述显示为默认值

**原因：** 元数据文件不存在或格式错误

**解决方案：**
1. 确认 `data/site-images-metadata.json` 文件存在
2. 检查JSON格式是否正确（可以用在线JSON验证工具）
3. 重新同步图片

---

### 问题4：同步图片时没有找到任何图片

**原因：** 图片路径或命名格式不正确

**解决方案：**
1. 检查图片是否在 `images/{分类}/` 目录下
2. 确认图片命名格式：`{prefix}-{编号}.webp`
3. 确认图片格式为 `.webp`
4. 检查浏览器控制台的错误信息

---

### 问题5：上传图片功能不可用

**当前状态：** 图片上传功能尚未完全实现

**解决方案：** 
使用FTP或文件管理器手动上传图片到 `images/{分类}/` 目录，然后在管理工具中点击 "同步网站图片"

---

## 📁 文件结构说明

```
网站根目录/
├── api-server.py              # 后端API服务器
├── START-SERVER.bat           # Windows启动脚本
├── START-SERVER.sh            # Mac/Linux启动脚本
│
├── data/                      # 数据目录
│   ├── site-images-metadata.json        # 主元数据文件
│   └── site-images-metadata.backup.json # 自动备份
│
├── admin/                     # 管理工具
│   ├── index.html            # 主页面
│   ├── scripts/
│   │   ├── admin-utils.js           # 工具函数
│   │   ├── site-sync-manager.js     # 同步管理器
│   │   ├── enhanced-image-manager.js # 图片管理器
│   │   └── profile-manager.js       # 个人信息管理器
│   └── styles/
│       └── admin.css         # 样式文件
│
├── scripts/                   # 网站脚本
│   └── unified-image-loader.js # 统一图片加载器
│
├── images/                    # 图片目录
│   ├── street/               # 街头摄影
│   ├── documentary/          # 人文纪实
│   ├── nature/               # 自然风光
│   ├── cityscape/            # 城市风光
│   ├── stilllife/            # 静物时光
│   ├── portrait/             # 人像摄影
│   └── animals/              # 动物摄影
│
└── pages/                     # 网站页面
    ├── street.html
    ├── documentary.html
    ├── nature.html
    └── ...
```

---

## 🎯 最佳实践

### 1. 定期备份
- 每周手动导出一次元数据
- 重大修改前先备份
- 将备份文件保存到云盘或其他安全位置

### 2. 命名规范
- **标题** - 简洁明了，格式：`地点·主题`
  - 好例子：`天津·赤峰桥`
  - 差例子：`这是一张在天津拍摄的关于赤峰桥的照片`

- **描述** - 详细但不冗长，50-100字为宜
  - 好例子：`高楼林立，车流光影，彰显天津璀璨夜景。`
  - 差例子：`图片`

### 3. 分类管理
- 每个图片只属于一个分类
- 选择最贴切的分类
- 如有疑问，参考现有图片的分类

### 4. 同步时机
- 上传新图片后
- 修改图片文件名后
- 删除图片文件后
- 怀疑数据不同步时

---

## 📞 技术支持

### 查看日志

**API服务器日志：**
- 终端/命令行窗口中会显示所有请求日志
- 保存成功会显示 `✓ 元数据已保存`
- 错误会显示 `✗ 保存失败` 及原因

**浏览器控制台：**
- 打开开发者工具（F12）
- 切换到 Console 标签
- 查看前端日志和错误信息

### 常用调试命令

在浏览器控制台中执行：

```javascript
// 查看当前加载的元数据
window.unifiedLoader.metadataCache

// 查看所有分类
window.unifiedLoader.categories

// 手动重新加载元数据
await window.unifiedLoader.loadMetadata()

// 查看管理工具中的图片数据
window.enhancedImageManager.images
```

---

## 🔄 版本更新日志

### v3.0 (2025-10-04)
- ✅ 统一标题和描述数据源
- ✅ 实现自动保存到服务器
- ✅ 优化管理工具界面（默认列表视图）
- ✅ 简化列表项显示
- ✅ 修复数据同步问题
- ✅ 网站优先读取JSON元数据

### v2.1 (之前版本)
- ✅ 实现图片扫描功能
- ✅ 优化为只扫描 .webp 格式
- ✅ 减少404错误

---

**最后更新：** 2025-10-04  
**版本：** v3.0  
**作者：** AI Assistant

