# 修复 501 错误

## 🚨 错误信息

```
::1 - - [04/Oct/2025 17:21:08] "POST /api/save-metadata HTTP/1.1" 501 -
```

**错误代码：** 501 - Unsupported method

---

## ✅ 已修复

我已经更新了 `api-server.py`，添加了更详细的调试信息。

### 修复内容：

1. **添加了异常捕获** - 更好的错误提示
2. **添加了调试日志** - 便于排查问题
3. **添加了Content-Length检查** - 防止空请求

---

## 🔄 请重新启动服务器

### 步骤1：停止当前服务器

在运行服务器的终端窗口中按 `Ctrl + C`

### 步骤2：重新启动服务器

**Windows：**
```bash
python api-server.py
```

**或者直接双击：**
```
START-SERVER.bat
```

### 步骤3：验证服务器启动

应该看到：
```
========================================================
🚀 摄影网站Admin工具 - API服务器
========================================================

✓ 服务器已启动: http://localhost:8000
✓ Admin工具地址: http://localhost:8000/admin/
✓ API端点: http://localhost:8000/api/save-metadata
```

---

## 🧪 测试保存功能

### 1. 打开管理工具

```
http://localhost:8000/admin/
```

### 2. 同步图片

点击 "同步网站图片" 按钮

### 3. 编辑一张图片

找到任意图片，点击 "编辑"，修改标题和描述

### 4. 点击保存

**期望看到：**
- 前端显示：`✓ 图片信息已保存`
- 终端显示：
  ```
  [DEBUG] POST请求路径: /api/save-metadata
  [DEBUG] 开始处理保存元数据请求
  [DEBUG] Content-Length: XXXX
  ✓ 元数据已保存: data/site-images-metadata.json
  ```

**如果还是501错误，终端会显示：**
```
[ERROR] POST请求处理失败: ...
```
这样我们就能知道具体是什么问题了。

---

## 🔍 常见原因和解决方案

### 原因1：端口被占用

**症状：** 服务器启动失败

**解决：**
```bash
# Windows - 查找占用8000端口的进程
netstat -ano | findstr :8000

# 结束进程（将PID替换为实际的进程ID）
taskkill /PID <PID> /F

# 重新启动服务器
python api-server.py
```

---

### 原因2：Python版本问题

**症状：** 代码运行错误

**检查：**
```bash
python --version
```

**要求：** Python 3.6+

**如果版本太低，使用：**
```bash
python3 api-server.py
```

---

### 原因3：文件权限问题

**症状：** 无法写入 `data/site-images-metadata.json`

**解决：**
```bash
# 检查 data 目录是否存在
mkdir data

# 检查文件权限（Mac/Linux）
chmod 755 data
chmod 644 data/site-images-metadata.json
```

---

### 原因4：JSON格式错误

**症状：** 保存时提示 "JSON解析失败"

**解决：**
1. 检查 `data/site-images-metadata.json` 文件
2. 使用在线JSON验证工具验证格式
3. 如果格式错误，删除文件让系统重新创建

---

## 📊 调试信息解读

### 正常的日志：

```
[DEBUG] POST请求路径: /api/save-metadata  ← 路径正确
[DEBUG] 开始处理保存元数据请求         ← 进入处理函数
[DEBUG] Content-Length: 1234            ← 请求体不为空
✓ 元数据已保存: data/site-images-metadata.json  ← 保存成功
  - 版本: 3.0
  - 最后更新: 2025-10-04...
  - 图片总数: 23
```

### 错误的日志：

```
[ERROR] 未知的API路径: /wrong-path  ← 路径错误
```

或

```
[ERROR] POST请求处理失败: ...  ← 异常错误
```

---

## 🆘 如果问题依然存在

### 请提供以下信息：

1. **完整的终端输出** - 包括错误信息
2. **操作系统** - Windows/Mac/Linux
3. **Python版本** - `python --version`
4. **浏览器控制台错误** - F12 → Console 标签

### 临时解决方案：

如果API保存一直失败，可以暂时使用手动导出：

1. 在管理工具点击 "手动导出元数据"
2. 下载 JSON 文件
3. 手动复制到 `data/site-images-metadata.json`
4. 刷新网站页面

---

## ✅ 验证修复成功

完成以下检查确认问题已解决：

- [ ] 服务器成功重启
- [ ] 终端显示 `[DEBUG]` 日志
- [ ] 点击保存后没有 501 错误
- [ ] 终端显示 `✓ 元数据已保存`
- [ ] `data/site-images-metadata.json` 文件已更新
- [ ] 网站显示修改后的内容

---

**现在请重新启动服务器并测试！** 🚀

如果还有问题，请把终端的完整输出发给我！

